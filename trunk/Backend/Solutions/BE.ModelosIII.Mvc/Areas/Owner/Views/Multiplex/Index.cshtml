@using BE.ModelosIII.Mvc.Models.Util
@model IEnumerable<BE.ModelosIII.Mvc.Models.Multiplex.MultiplexModel>
@{
    ViewBag.Title = "Complejos";
    Layout = ViewBag.Layout;
}
<section class="utopia-widget">
    <div class="utopia-widget-title">
        <img class="utopia-widget-icon" src="/Content/img/icons/paragraph_justify.png">
        <span>Listado de Complejos</span>
    </div>
    <div class="utopia-widget-content">
        @if (ViewBag.Actions != null)
        {
            @Html.Partial("_Actions", (IEnumerable<ActionModel>)ViewBag.Actions)
        }
           
        <table class="table datatable table-striped table-bordered">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Dirección</th>
                    <th>Ciudad</th>
                    <th>Colectivos</th>
                    <th>Subtes</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tfoot>
                <tr>
                    <th>Nombre</th>
                    <th>Dirección</th>
                    <th>Ciudad</th>
                    <th>Colectivos</th>
                    <th>Subtes</th>
                    <th>Acciones</th>
                </tr>
            </tfoot>
            <tbody>
                @foreach (var multiplexModel in Model)
                {
                    <tr>
                        <td>@multiplexModel.Name</td>
                        <td>@multiplexModel.Address</td>
                        <td>@multiplexModel.City</td>
                        <td>@multiplexModel.Buses</td>
                        <td>@multiplexModel.Subways</td>
                        <td class="actions">
                             <a class="edit" data-original-title="Ver Salas" href="@Url.Action("ScreenIndex", new { Id = multiplexModel.Id })">
                                <img alt="Edit" src="/Content/img/icons/eye.png"></a>
                            <a class="edit" data-original-title="Editar" href="@Url.Action("Edit", new { Id = multiplexModel.Id })">
                                <img alt="Edit" src="/Content/img/icons/pencil.png"></a>
                            <a class="delete" data-original-title="Borrar" href="@Url.Action("Delete", new { Id = multiplexModel.Id })">
                                <img alt="Delete" src="/Content/img/icons/trash_can.png"></a>
                        </td>   
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>

@section scripts {
    <script type="text/javascript" src="/Content/js/tables.js"></script>
    <script type="text/javascript">
        $(function () {
            initializeDataTableOnLoad('.datatable');
        });
    </script>
}
